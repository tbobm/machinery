{
    "name": "reverse",
    "address": "http://reverse.local:5000/",
    "inputs": {
        "message": {
            "type": "string",
            "description": "The text to transform"
        }
    },
    "outputs": {
        "message": {
            "type": "string",
            "description": "The transformed text, in reverse"
        }
    }
}
